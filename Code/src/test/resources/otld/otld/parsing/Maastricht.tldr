City Maastricht;

Begin depot;
    Wagon a accepts int;
    Wagon b accepts boolean;
    Wagon c accepts bool;
End depot;

Begin company;
    Transport a to factory bad and fully load b;
End company;

/** We expect a syntax error, nothing more since syntax error should stop the walking of the tree */

